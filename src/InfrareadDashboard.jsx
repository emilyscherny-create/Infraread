import React from "react";

/**
 * Minimal placeholder Dashboard component.
 * The original file contained unwrapped JSX/HTML fragments which caused a syntax error
 * during the Vite/esbuild transform. This minimal component unblocks the build.
 */

export default function InfrareadDashboard() {
  // Return null so the dashboard doesn't render anything for now.
  // This keeps the import in src/App.jsx valid and unbroken.
  return null;
}